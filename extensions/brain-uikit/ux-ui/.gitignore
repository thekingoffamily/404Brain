# Locally-installed agent skill bundles — never commit to this repo
.agents/
skills-lock.json

# node / npm
node_modules/
*.tgz
npm-debug.log*

# Local-only demos (IP) — never commit
examples/apple-home/

# Personal, machine-local preferences — never commit
CLAUDE.local.md

# eval outputs are committed as fixtures: they are what the 13-gate scorer runs against

# python bytecode
__pycache__/
*.pyc

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
dist/
